# Copier en .env.dev (ne pas committer .env.dev). MySQL exige des valeurs non vides.
# Lancer : docker compose --env-file .env.dev -f docker-compose.dev.yml up -d

MYSQL_ROOT_PASSWORD=
MYSQL_PASSWORD=
